@{
    ViewData["Title"] = "ITBusinessCase";
}

<div class="container py-5">
    <div class="row align-items-center g-4">
        <div class="col-lg-6">
            <h1 class="display-5 fw-bold">Welkom bij ITBusinessCase</h1>
            <p class="lead text-muted">
                Plaats een bestelling via onze checkout. Na het verzenden wordt je order via MassTransit
                gepubliceerd naar RabbitMQ.
            </p>

            <div class="d-flex flex-wrap gap-2 mt-4">
                <a class="btn btn-success btn-lg px-4" href="/Orders/Create">
                    Nieuwe bestelling
                </a>

                <a class="btn btn-outline-secondary btn-lg px-4" href="http://localhost:15672" target="_blank">
                    RabbitMQ dashboard
                </a>
            </div>

            <div class="mt-4 small text-muted">
                Tip: Wil je de messages “blijven zien” in RabbitMQ? Zet je consumer tijdelijk uit.
            </div>
        </div>

        <div class="col-lg-6">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">
                    <h5 class="fw-semibold mb-3">Hoe werkt het?</h5>

                    <ol class="mb-0 text-muted">
                        <li>Je vult het formulier in</li>
                        <li>Controller publish’t <code>OrderSubmitted</code></li>
                        <li>MassTransit stuurt naar RabbitMQ</li>
                        <li>Consumer verwerkt de order</li>
                    </ol>
                </div>
            </div>

            <div class="row g-3 mt-3">
                <div class="col-6">
                    <div class="card border-0 shadow-sm rounded-4">
                        <div class="card-body p-3">
                            <div class="fw-semibold">MassTransit</div>
                            <div class="text-muted small">Messaging framework</div>
                        </div>
                    </div>
                </div>
                <div class="col-6">
                    <div class="card border-0 shadow-sm rounded-4">
                        <div class="card-body p-3">
                            <div class="fw-semibold">RabbitMQ</div>
                            <div class="text-muted small">Broker / queues</div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
